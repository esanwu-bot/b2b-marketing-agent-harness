# 开发计划 · B2B Marketing Agent Harness

> 配套文档：`项目需求.md`（需求基线）、`schema.sql`（PostgreSQL 建表）。
> 本计划按「可跑通 → 可观测 → 可扩展 → 接真渠道」的风险递增顺序推进。

- 文档状态：V1 开发计划
- 起点：M0 需求/schema/骨架（已完成）
- 技术栈：Go 1.23+ / PostgreSQL 16 / Redis / Vue 3 + Vben

---

## 1. 总体架构

```
                    ┌──────────────────────────┐
                    │   Admin Console (Vue3)    │  控制面
                    └────────────┬─────────────┘
                                 │ REST + SSE
                    ┌────────────▼─────────────┐
                    │      API Gateway          │
                    └────────────┬─────────────┘
        ┌────────────────────────┼─────────────────────────┐
        ▼                        ▼                         ▼
  Agent Manager            Workflow Engine            Knowledge
        └────────────────────────┼─────────────────────────┘
                                 ▼
                       ┌───────────────────┐
                       │   Agent Runtime   │  Plan→Context→Execute→Observe→Reflect
                       └─────────┬─────────┘
                    ┌────────────┴────────────┐
                    ▼                         ▼
              Tool Registry             Policy Engine
                    │
     ┌──────────────┼───────────────────────────┐
     ▼              ▼                            ▼
 Crawl Tool    LinkedIn Tool              Knowledge Tool
     │              │                            │
 crawl-worker   LinkedIn API               PostgreSQL(+pgvector)
   -redis(MySQL)                                 Redis(Queue/Event)
```

**分层边界（不可破坏）：**
- Harness 不感知行业；行业差异只出现在 Agent 规格 / Workflow / 知识库。
- Harness 不直连 crawler 的 MySQL；只通过 Tool。
- Workflow 决定「何时做」，Agent 决定「怎么做」，Tool「真正执行」。

---

## 2. 技术栈

| 层 | 选型 | 备注 |
|----|------|------|
| 语言 | Go 1.23+ | 与 goKit 一致 |
| API | 标准库 `net/http`（Go 1.22 ServeMux 路由）+ SSE | 不引重框架 |
| DB | PostgreSQL 16 | Source of Truth；已建库 `b2b_marketing_agent` |
| 向量 | pgvector（可选） | V1 embedding 存 JSONB，装 pgvector 后 ALTER 升级 |
| 队列/缓存/事件/锁 | Redis 8 (Streams) | 无 Redis 时可退化内存实现 |
| LLM | OpenAI / Anthropic / Gemini / OpenAI-compatible | 内置 `mock` provider 离线可跑 |
| Tool 协议 | Native + MCP（占位） | 统一 Tool 接口 |
| Crawler | crawl-worker-redis API | 通过 Crawler Tool 适配 |
| 前端 | Vue 3 + Vben Admin | M3 起 |
| 观测 | OpenTelemetry（M3+） | V1 先落结构化表 + 结构化日志 |
| 部署 | Docker Compose | |

---

## 3. 项目目录（目标）

```
b2b-marketing-agent-harness/
├── cmd/
│   ├── api/                  # REST + SSE 服务
│   ├── worker/               # 任务消费 / Agent 执行
│   ├── scheduler/            # Cron 调度
│   └── harness/              # 本地 demo CLI（无依赖跑全链路）
├── internal/
│   ├── domain/               # 契约层（零外部依赖）
│   ├── agent/
│   │   ├── runtime/          # Agent 循环
│   │   ├── planner/          # LLM / Rule Planner
│   │   ├── executor/         # Plan 执行
│   │   ├── contextbuilder/   # Context 组装 + 裁剪
│   │   ├── memory/           # Memory 管理
│   │   └── registry/         # Agent 注册表
│   ├── tool/
│   │   ├── registry/         # Tool 注册 + 路由
│   │   ├── crawler/          # 对接 crawl-worker-redis
│   │   ├── content/          # 生成/翻译/改写
│   │   ├── knowledge/        # 存取/检索
│   │   ├── linkedin/         # 渠道（V1 mock）
│   │   └── notification/     # 通知
│   ├── llm/
│   │   ├── provider/         # Provider 接口
│   │   ├── mock/             # 离线确定性 provider
│   │   └── openai/           # OpenAI-compatible
│   ├── workflow/
│   │   ├── definition/       # Workflow 定义解析
│   │   ├── engine/           # 顺序/分支执行
│   │   └── scheduler/        # Cron
│   ├── knowledge/            # 文档/分块/检索
│   ├── content/              # 选题/草稿/资产
│   ├── policy/               # 策略引擎
│   ├── store/
│   │   ├── store.go          # 存储接口
│   │   ├── memory/           # 内存实现（mock，MVP）
│   │   └── postgres/         # PG 实现（M1 末接入）
│   ├── observability/        # 日志/trace/cost
│   ├── config/               # 配置
│   └── transport/httpapi/    # 路由与处理器
├── examples/semiconductor/   # 参考实现：Agent 规格 + Workflow + 种子数据
├── migrations/               # schema.sql / 增量迁移
├── configs/config.yaml
├── docs/
├── Makefile
├── docker-compose.yml
├── go.mod
└── README.md
```

---

## 4. 分阶段计划

### M0 · 需求 / 架构 / Schema / 骨架 ✅

- [x] `项目需求.md`、`开发计划.md`
- [x] `schema.sql`（34 表 + 1 视图，已在 PG16 执行通过）
- [x] 目录骨架、go.mod、README、LICENSE、Makefile、docker-compose、configs
- [x] `.env`（本地连接，gitignore）

**验收**：`psql -d b2b_marketing_agent` 可见全部表；`go build ./...` 通过。

### M1 · Harness 核心 + Mock 全链路 + REST API

- [ ] domain 契约：Agent/Task/Run/Step/Tool/Workflow/Memory/Knowledge/Policy/Event
- [ ] LLM Provider 抽象 + `mock` provider + `openai` provider
- [ ] Tool Registry + Tool 接口 + `tool_executions` 记录
- [ ] Agent Runtime：Plan→Context→Execute→Tool→Observe→Reflect，带 RunPolicy
- [ ] Planner：Rule（离线）+ LLM（结构化输出）
- [ ] ContextBuilder（System+Profile+Task+Memory+Knowledge+Tool 结果）
- [ ] State（可 Resume）、Observability（结构化 Run Trace）
- [ ] store 接口 + 内存实现（mock），种子数据
- [ ] REST API：agents/tools/tasks/runs/approvals/knowledge/topics/content/metrics + SSE
- [ ] `cmd/harness` demo：离线跑通一个 Agent Run

**验收**：`make demo` 离线跑通；API 能看到 run/step/tool/llm 时间线。

### M2 · 6 个参考 Agent + 每日情报 Workflow

- [ ] Research / Trend / Strategy / Content / Review / Analytics Agent
- [ ] Workflow Engine（agent/approval/channel/delay/condition）+ `daily_semiconductor_intelligence`
- [ ] crawler Tool（先 Mock HTTP，能接 crawl-worker-redis 的 `/api/v1`）
- [ ] LinkedIn Tool（mock 发布）+ content/knowledge/notification Tool
- [ ] Approval 落库与流程挂起/恢复

**验收**：一条 Workflow 从采集→选题→生成→审核→（审批）→发布→分析全链路跑通。

### M3 · Policy / 可观测 / 后台控制台

- [ ] Policy Engine（低/中/高风险分级）+ audit_logs
- [ ] OpenTelemetry Trace + 成本统计
- [ ] SSE 时间线完善（Agent 思考 / Tool Calling）
- [ ] Vue 3 + Vben 控制台：Dashboard / Agents / Runs / Workflows / Approvals / Content / Knowledge

**验收**：后台可视化审批与 Run 回放。

### M4 · 接真实 crawl-worker-redis

- [ ] Crawler 适配器：create_job / get_job / results
- [ ] 采集结果 → knowledge_documents（去重/分块）
- [ ] 事件回流（采集完成 → 触发 Trend Agent）

**验收**：真实采集一批半导体新闻入库并驱动选题。

### M5 · LinkedIn 真实发布

- [ ] OAuth 凭据管理（channel_credentials 加密）
- [ ] Company Page API：发帖/图片/排期/指标
- [ ] 发布回执与异常重试

**验收**：真实账号发布一条内容并回收曝光/互动。

### M6 · 评估与调度

- [ ] Eval Harness：golden 集、回归、A/B（内容/提示词）
- [ ] Guardrail（术语表、事实校验、敏感词）
- [ ] Scheduler 完整（cron/时区/幂等/死信）

---

## 5. 核心 Go 接口（契约草案）

```go
// domain.Agent
type Agent interface {
    ID() string
    Name() string
    Run(ctx context.Context, task *Task) (*Result, error)
}

// domain.Tool
type Tool interface {
    Name() string
    Description() string
    InputSchema() Schema
    Risk() string            // read | write | external
    RequiresApproval() bool
    Execute(ctx context.Context, input map[string]any) (*ToolResult, error)
}

// agent/planner
type Planner interface {
    Plan(ctx context.Context, rc *RunContext) (*Plan, error)
}

// agent/executor
type Executor interface {
    Execute(ctx context.Context, rc *RunContext, plan *Plan) (*Result, error)
}

// llm/provider
type Provider interface {
    Name() string
    Complete(ctx context.Context, req ChatRequest) (*ChatResponse, error)
}

// store
type Store interface {
    // agents / tasks / runs / steps / content / knowledge / approvals / workflows / events
}
```

`RunPolicy`：

```go
type RunPolicy struct {
    MaxSteps     int
    MaxTokens    int
    Timeout      time.Duration
    MaxToolCalls int
    MaxCost      float64
    RequireApproval bool
}
```

`AgentState`（可持久化/Resume）：

```go
type AgentState struct {
    RunID       string
    TaskID      string
    AgentID     string
    Status      string
    CurrentStep int
    Variables   map[string]any
    Messages    []Message
    ToolCalls   []ToolCall
    CreatedAt   time.Time
    UpdatedAt   time.Time
}
```

---

## 6. 数据库映射（schema.sql → 代码）

| 代码契约 | 表 | 备注 |
|----------|----|----|
| `AgentSpec` | `agents` / `agent_versions` | 规格 + 版本 |
| `Tool` | `tools` / `tool_executions` | 注册 + 执行 |
| `Task` | `tasks` | 调度单元 |
| `Run` / `Step` / `Message` / `Decision` | `task_runs` / `task_steps` / `agent_messages` / `decisions` | 执行链路 |
| `LLMCall` | `llm_calls` | 成本/tokens |
| `Workflow` / `Run` / `Step` | `workflows` / `workflow_runs` / `workflow_steps` | 编排 |
| `Memory` | `memories` | scope 隔离 |
| `Document` / `Chunk` / `Entity` | `knowledge_documents` / `knowledge_chunks` / `knowledge_entities` / `knowledge_relations` | 知识 |
| `Topic` / `Content` / `Publication` / `Metric` | `topics` / `content` / `content_versions` / `content_assets` / `publications` / `content_metrics` | 内容域 |
| `Engagement` / `Lead` | `engagements` / `engagement_replies` / `leads` | 互动/线索 |
| `Approval` | `approvals` | HITL |
| `Event` / `Schedule` | `events` / `schedules` | 事件/调度 |

---

## 7. Redis 设计

| Key | 类型 | 用途 |
|-----|------|------|
| `bh:tasks` | Stream | 待执行 Task 队列（消费组 `workers`） |
| `bh:tasks:dead` | Stream | 死信 |
| `bh:events` | Stream | 领域事件总线 |
| `bh:run:{id}:state` | String(JSON) | Run 状态缓存 |
| `bh:lock:{scope}` | String(SETNX) | 分布式锁（同选题/同账号串行） |
| `bh:cache:*` | String | 缓存 |

无 Redis 时：`store/memory` + 内存队列，保证离线可跑。

---

## 8. API 草案

```
GET  /api/v1/health
GET  /api/v1/agents                 POST /api/v1/agents
GET  /api/v1/tools
GET  /api/v1/workflows              POST /api/v1/workflows
POST /api/v1/workflows/{id}/run
GET  /api/v1/tasks                  POST /api/v1/tasks
GET  /api/v1/runs                   GET  /api/v1/runs/{id}
GET  /api/v1/runs/{id}/steps        GET /api/v1/runs/{id}/stream   (SSE)
GET  /api/v1/approvals              POST /api/v1/approvals/{id}/approve|reject
GET  /api/v1/knowledge              POST /api/v1/knowledge
GET  /api/v1/topics                 POST /api/v1/topics
GET  /api/v1/content                POST /api/v1/content
GET  /api/v1/metrics              GET  /api/v1/events
```

---

## 9. 里程碑与验收对照

| 里程碑 | 核心验收 |
|--------|----------|
| M0 | schema 建库成功；骨架可 build |
| M1 | `make demo` 离线跑通；API 返回 Run 时间线 |
| M2 | 完整每日情报 Workflow 跑通（含审批挂起） |
| M3 | 后台可视化审批 + Run 回放 |
| M4 | 真实 crawler 数据入库并驱动选题 |
| M5 | 真实 LinkedIn 发布 + 指标回收 |
| M6 | Eval/Guardrail/调度完整 |

---

## 10. 风险与对策

| 风险 | 对策 |
|------|------|
| LLM 成本失控 | RunPolicy 限额 + `llm_calls` 计费 + 模型路由 |
| Agent 失控循环 | MaxSteps/MaxToolCalls/Timeout + Policy |
| 技术事实错误 | Review Agent + 术语表knowledge + 强制人工审批 |
| LinkedIn API 资质 | V1 用 Mock，接口先定型；发布默认需审批 |
| 与 crawler 耦合 | 只保留 `Crawler` 接口 + Tool 适配，禁止跨库 |
| 过度设计 | V1 不引入独立向量/图/搜索引擎 |
| PG 扩展缺失 | 扩展创建容错；embedding 先 JSONB，后升级 pgvector |

---

## 11. 开发约定

- 中英文注释均可，领域术语保留英文；导出符号必须有注释。
- 契约（`internal/domain`）零外部依赖，实现可替换。
- 所有外部副作用（发布/采集/LLM）必须走 Tool / Provider，便于 mock 与观测。
- 提交信息：`feat(scope): 描述` / `fix(scope): 描述`。
- `make fmt/vet/test/build/demo` 保持全绿。

---

## 12. 当前进度

- M0：**已完成**（schema 已在 PG16 `b2b_marketing_agent` 执行，34 表 + 1 视图）。
- M1：**进行中**（Harness 核心 + mock 全链路 + REST API）。
