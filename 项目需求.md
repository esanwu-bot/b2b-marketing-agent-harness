# 项目需求 · B2B Marketing Agent Harness

> 一个面向 B2B 半导体 / 电子元器件 / 工业科技行业的**开源营销智能体脚手架（Agent Harness）**。
> 它不绑定某一渠道：其中 **LinkedIn** 是 V1 的第一个落地渠道（参考对象：Microchip Taiwan 官网 / LinkedIn 主页的运营形态）。
>
> 与数据采集项目的边界（详见第 12 章）：
> **crawl-worker-redis 是 Agent 的“手”（How to collect），本项目是“大脑 + 神经系统”（Why / What / Decide）。**

- 文档状态：V1 需求基线
- 关联仓库：`crawl-worker-redis`（数据采集基础设施，MySQL）
- 本仓库：`b2b-marketing-agent-harness`（营销 Agent 基础设施，PostgreSQL）

---

## 1. 背景与目标

### 1.1 背景

B2B 半导体 / 元器件公司的市场团队（如 Microchip Taiwan）持续产出并运营大量内容：

- 产品新闻、公司新闻、技术博客、白皮书、应用案例
- 线上/线下研讨会（RTC / eRTC / Webinar）、展会、校园课程
- 技术 FAQ、开发工具、参考设计

这些内容分散在官网、LinkedIn、Facebook、YouTube、Newsletter。人工运营面临三个核心问题：

1. **信息采集成本高**：行业动态、竞品内容、技术趋势需要人工持续跟踪。
2. **内容生产不稳定**：选题依赖个人经验，品牌口吻、术语、技术事实难以统一。
3. **缺少闭环**：发布后的数据（曝光、互动、线索）没有回流到下一轮选题与策略。

### 1.2 目标

构建一个**可持续运行、可观测、可扩展、Human-in-the-loop** 的营销 Agent 系统：

```
Research → Intelligence → Content → Approval → Publishing → Analytics → Optimization
```

它由三层组成：

- **Agent Harness（本项目的核心资产）**：Agent Runtime / Planner / Tool Registry / Memory / Knowledge / Policy / Context / State / Workflow / Observability，**与行业无关**。
- **Semiconductor Marketing Reference Implementation（参考实现）**：6 个内置 Agent + 示例 Workflow，落在 `examples/semiconductor`。
- **Channel / Tool 适配层**：LinkedIn、官网、crawler 等以 Tool 形式接入，可替换。

### 1.3 一句话定位

> **Domain-independent Agent Harness + Semiconductor Marketing Reference Implementation**
> —— 让“拿到数据以后，Agent 如何理解、决策、生成、执行”成为可复用、可观测、可扩展的基础设施。

---

## 2. 设计原则

| # | 原则 | 说明 |
|---|------|------|
| 1 | **Agent ≠ Workflow** | Workflow 决定“什么时候做什么”，Agent 决定“具体怎么做”，Tool 负责“真正执行” |
| 2 | **契约先行** | `Agent / Tool / Planner / Memory / Knowledge / Policy / State` 全部先定接口，实现可替换 |
| 3 | **Domain-independent Harness** | Harness 不认知半导体；行业知识只出现在 system prompt / workflow / 知识库 |
| 4 | **Human-in-the-loop** | 对外发布、删除、私信、建活动等写操作必须经 Policy + 审批 |
| 5 | **一切皆结构化数据** | Agent Run / Step / Tool Call / Decision / Approval / Content / Event 全落库，形成 Workflow Data Layer |
| 6 | **两个仓库解耦** | Harness 不直连 crawler 的 MySQL；只通过 Tool / API / Event 交互 |
| 7 | **V1 从简** | 不引入独立向量库/图数据库/搜索引擎；PostgreSQL(+pgvector) + Redis 足够 |
| 8 | **可离线运行** | 内置 mock LLM / mock store / mock channel，无外部依赖也能跑通全链路 |

---

## 3. 用户与场景

### 3.1 用户角色

| 角色 | 关注点 | 主要操作 |
|------|--------|----------|
| 市场运营（Operator） | 选题、内容、排期、互动 | 审批内容、编辑草稿、排期发布、处理评论 |
| 技术市场（Tech Marketing） | 技术事实、品牌口吻 | 审核技术准确性、维护术语与品牌规范 |
| 增长 / 销售 | 线索 | 查看互动中的潜在线索、转交 CRM |
| 开发者 / 平台方 | 扩展 Agent/Tool/Channel | 注册 Agent、开发 Tool、接新渠道、看 Trace |
| 管理者 | ROI、成本、质量 | 看 KPI、成本、Agent 表现、内容表现 |

### 3.2 核心场景

1. **每日行业情报**：定时采集新闻/竞品内容 → 聚类趋势 → 生成选题 → 打分 → 进入选题池。
2. **内容生产**：从选题生成 LinkedIn 多语言草稿 → 技术事实/品牌审核 → 人工审批 → 排期发布。
3. **互动管理**：抓取评论/私信 → 意图识别（问题/线索/投诉/垃圾）→ 生成回复草稿 → 审批 → 发送/转交。
4. **数据回流**：发布后按周期拉取指标 → 归因到选题/格式/语言 → 产出优化建议 → 影响下一轮选题。
5. **能力扩展**：新增 SEO/GEO/Sales/Support Agent 或新渠道（YouTube/Newsletter）而不改 Runtime。

---

## 4. 范围（V1）

### 4.1 V1 包含

- Agent Harness 核心：Runtime（Plan→Context→Execute→Tool→Observe→Reflect）、Planner、Executor、Tool Registry、Context Builder、State、Policy Engine、Memory、Knowledge、Observability。
- 6 个内置 Agent：Research / Trend / Strategy / Content / Review / Analytics。
- Workflow Engine：顺序编排（agent / approval / channel / delay / condition），Cron 触发。
- Human-in-the-loop：Approval 队列 + Policy 规则（低风险自动、中风险 AI 审、高风险人工）。
- Channel：**LinkedIn**（V1 先做 Mock + 适配器接口，再接真实 Company Page API）。
- Tool：Crawler（接 crawl-worker-redis）、Content（生成/翻译/改写）、Knowledge（存取/检索）、Notification、Browser（Mock）。
- 存储：PostgreSQL（Source of Truth）+ Redis（Queue/Cache/Lock/Event）。
- API：REST + SSE（Agent Run 时间线实时推送）。
- 参考实现：`examples/semiconductor`（工作区、Agent 规格、示例 Workflow、Mock 数据）。

### 4.2 V1 不包含（Out of Scope）

- 通用 Agent 平台（任意行业任意流程的可视化编排）。
- 真实 LinkedIn 生产发布（V1 先 Mock，真实发布需企业 API 资质，作为 V1.5）。
- 独立向量数据库 / 图数据库 / 搜索引擎 / 数据仓库。
- 多租户计费、SSO、细粒度 RBAC（V1 用工作区 + 简单鉴权占位）。
- 模型微调 / 自建推理。

### 4.3 非目标（明确不做）

- 不做“LinkedIn 自动发帖机器人”。LinkedIn 只是 Channel 之一。
- 不把 Agent / LinkedIn / Workflow 塞进 `crawl-worker-redis`。

---

## 5. 功能需求

### 5.1 Agent Runtime（核心）

| 编号 | 需求 | 说明 |
|------|------|------|
| FR-RT-1 | 统一 Agent 接口 | `ID / Name / Run(ctx, Task) → Result` |
| FR-RT-2 | 标准循环 | Receive→Load Context→Plan→Execute→Tool→Observe→Reflect→(Done?) |
| FR-RT-3 | 运行预算 | `RunPolicy{MaxSteps, MaxTokens, Timeout, MaxToolCalls, MaxCost, RequireApproval}`，禁止无限循环 |
| FR-RT-4 | 状态可恢复 | 保存 `AgentState`，中途失败可 Resume 而非重跑 |
| FR-RT-5 | 可替换组件 | Planner/Executor/ToolRegistry/Memory/Knowledge/Policy/Observer 均为接口 |
| FR-RT-6 | 完整 Trace | 每次 Run 记录 steps / messages / tool_calls / llm_calls / decisions |

### 5.2 Planner

- FR-PL-1：只产出 `Plan`（步骤列表），不执行。
- FR-PL-2：支持 LLM Planner（结构化输出）与 Rule Planner（离线/降级）。
- FR-PL-3：支持 Reflect 后重规划（replan）。

### 5.3 Tool Registry

- FR-TL-1：统一接口 `Name/Description/InputSchema/Execute`。
- FR-TL-2：支持 Native / MCP / HTTP / gRPC 多来源（V1 至少 Native + HTTP + MCP 占位）。
- FR-TL-3：Tool 声明 `risk`（read/write/external）与 `requires_approval`。
- FR-TL-4：所有执行写入 `tool_executions`（输入/输出/耗时/状态）。

### 5.4 Context Engine

- FR-CX-1：组装 `System Prompt + Agent Profile + Task + Conversation + Workflow State + Memory + Knowledge + Tool Result + Previous Actions`。
- FR-CX-2：Token 预算裁剪（按重要性/时效性）。

### 5.5 Memory / Knowledge（必须分开）

- FR-MK-1 Memory 记录“过去发生了什么”（偏好/事实/摘要/情景），按 scope 隔离（agent/user/workspace/task）。
- FR-MK-2 Knowledge 记录“世界上有什么知识”（文档/分块/实体/关系）。
- FR-MK-3 Knowledge 支持全文检索（V1：PostgreSQL + pg_trgm；V1.5：pgvector）。

### 5.6 Policy Engine

- FR-PO-1：规则化策略，如 `linkedin.publish.require_approval=true`、`crawler.fetch.require_approval=false`。
- FR-PO-2：风险分级：低→自动执行，中→AI 复核，高→人工审批。
- FR-PO-3：每次决策写入 `decisions` / `audit_logs`。

### 5.7 Workflow Engine

- FR-WF-1：Workflow 定义（JSONB/YAML），步骤类型：agent / approval / channel / delay / condition。
- FR-WF-2：Cron 触发（scheduler）+ 手动触发。
- FR-WF-3：失败重试 / 死信 / 幂等（Redis Stream 或 DB 状态机）。
- FR-WF-4：Workflow Run 全程可观测。

### 5.8 内容域

- FR-CT-1：Topic 管理（候选/通过/拒绝/已用），来源可追溯到 Knowledge/采集结果。
- FR-CT-2：Content 支持多语言、多格式（post/article/video_script/newsletter）、版本化。
- FR-CT-3：Content 资产（图片/视频/封面）管理。
- FR-CT-4：Publication 排期/发布/回执；Metrics 周期采集与归因。

### 5.9 互动与线索

- FR-EN-1：抓取评论/提及/私信，意图与情感识别。
- FR-EN-2：生成回复草稿，经审批后发送。
- FR-EN-3：高意向互动转 `leads`，可转交外部 CRM。

### 5.10 审批（Human-in-the-loop）

- FR-AP-1：审批队列，支持 approve / reject / edit / expire。
- FR-AP-2：审批对象：发布、回复、删除、私信、建活动等。
- FR-AP-3：审批结果驱动 Run/Workflow 继续或终止。

### 5.11 API / 后台

- FR-AP-4：REST API（工作区/Agent/Tool/Task/Run/Workflow/Knowledge/Topic/Content/Approval/Metrics）。
- FR-AP-5：SSE 推送 Agent 思考 / Tool Calling 时间线。
- FR-AP-6：后台控制台（Vue 3 + Vben Admin，后续阶段）。

### 5.12 集成

- FR-IN-1：Crawler Tool 对接 `crawl-worker-redis`（创建作业/查进度/取结果）。
- FR-IN-2：LLM Provider：OpenAI / Anthropic / Gemini / OpenAI-compatible，可路由。
- FR-IN-3：LinkedIn Channel：Mock（V1）→ Company Page API（V1.5）。
- FR-IN-4：Notification：Email / Webhook / 企业微信 / Slack。

---

## 6. 非功能需求

| 类别 | 要求 |
|------|------|
| 可扩展 | 新增 Agent/Tool/Channel/Workflow 不改 Runtime；Harness 与行业解耦 |
| 可观测 | OpenTelemetry Trace + `agent_*` 结构化表；每次 Run 可回放 |
| 成本控制 | RunPolicy 限制 tokens/cost；`llm_calls` 记录费用 |
| 可靠性 | 幂等、重试、死信；Agent State 可恢复 |
| 安全 | 凭据加密存储；写操作审批；审计日志；Prompt 注入防护 |
| 国际化 | 内容多语言（zh-TW / en 为主），术语表统一 |
| 可移植 | Docker Compose 一键起；无 PG/Redis 时可用 Mock 模式跑通 |
| 许可 | 开源（MIT），README 第一屏强调领域无关 + 半导体参考实现 |

---

## 7. 内置 Agent（V1，6 个）

| Agent | 职责 | 主要 Tool | 产出 |
|-------|------|-----------|------|
| **Research Agent** | 行业/竞品信息采集 | crawler.*、browser.*、knowledge.store | 结构化情报文档 |
| **Trend Agent** | 趋势发现与聚类 | knowledge.search、llm.* | 趋势/主题簇 |
| **Strategy Agent** | 选题与打分 | knowledge.search、llm.* | 排序后的 Topic 列表 |
| **Content Agent** | LinkedIn/官网内容生成 | content.generate/translate、knowledge.search | 多语言草稿 |
| **Review Agent** | 技术事实 + 品牌审核 | knowledge.search、llm.* | 审核结论/修改建议 |
| **Analytics Agent** | 数据回流与优化建议 | linkedin.get_analytics、llm.* | 优化建议、选题反馈 |

> Agent Graph（进阶）：Research → (News/LinkedIn/Trend) → Topic → Strategy → Writer → Review → Human Approval → Publisher → Analytics → Optimization。

---

## 8. 关键流程

### 8.1 每日行业情报与选题

```
Cron 08:00
  → Workflow: daily_semiconductor_intelligence
    → Research Agent（采集新闻/竞品）
    → Trend Agent（聚类趋势）
    → Strategy Agent（选题打分）
    → 产出 Topic 候选池
```

### 8.2 内容生产到发布

```
Topic（已通过）
  → Content Agent（生成草稿，多语言）
  → Review Agent（技术 + 品牌审核）
  → Approval（人工审批，可编辑）
  → Publication（排期到 LinkedIn）
  → Analytics（发布后回流）
```

### 8.3 互动与线索

```
Engagement（评论/私信）
  → 意图识别
  → Reply 草稿 → Approval → 发送
  → 高意向 → Lead → CRM
```

---

## 9. 数据模型（与 schema.sql 对应）

核心链路（最重要）：

```
task → task_run → task_step → tool_execution / llm_call / decision
workflow → workflow_run → workflow_step → task
```

主要表分组：

| 分组 | 表 |
|------|----|
| 工作区/渠道 | `workspaces`、`channels`、`channel_credentials` |
| Agent | `agents`、`agent_versions` |
| Tool | `tools`、`tool_executions` |
| 执行 | `tasks`、`task_runs`、`task_steps`、`agent_messages`、`decisions`、`llm_calls` |
| Workflow | `workflows`、`workflow_runs`、`workflow_steps` |
| Memory | `memories` |
| Knowledge | `knowledge_documents`、`knowledge_chunks`、`knowledge_entities`、`knowledge_relations` |
| 内容 | `topics`、`content`、`content_versions`、`content_assets`、`publications`、`content_metrics` |
| 互动/线索 | `engagements`、`engagement_replies`、`leads` |
| 审批 | `approvals` |
| 调度/事件/审计 | `schedules`、`events`、`audit_logs` |

---

## 10. 技术栈（V1 基线）

| 层 | 技术 |
|----|------|
| Language | Go 1.23+ |
| API | REST + SSE |
| Agent Runtime | 自研 Harness |
| Workflow | 自研 Workflow Engine |
| DB | PostgreSQL（+ pgvector） |
| Queue/Cache/Event/Lock | Redis（Streams） |
| Object Storage | S3 / MinIO |
| LLM | OpenAI / Anthropic / Gemini / OpenAI-compatible |
| Tool Protocol | Native Tool + MCP |
| Crawler | crawl-worker-redis |
| Observability | OpenTelemetry |
| Frontend | Vue 3 + Vben Admin |
| Deployment | Docker Compose → Kubernetes |

---

## 11. 验收标准（V1）

1. **Harness 可跑**：`make demo` 能离线（mock LLM + mock store）完整跑通
   `Research → Trend → Strategy → Content → Review → Approval → Publication(LinkedIn mock) → Analytics`。
2. **契约完整**：Agent/Tool/Planner/Memory/Knowledge/Policy/Workflow 均为接口，且有 mock 实现。
3. **可观测**：一次 Run 可在 API 看到 step/tool/llm/decision 全时间线。
4. **可审批**：发布类动作在无审批时进入 `awaiting_approval`，审批后继续。
5. **可扩展**：新增一个 Agent/Tool 不需要修改 Runtime 代码。
6. **数据边界**：Harness 不直连 crawler MySQL；Crawler 通过 Tool/API 调用。
7. **schema.sql**：可在 PostgreSQL 16 上直接执行建库（无 pgvector 亦可）。

---

## 12. 与 crawl-worker-redis 的边界（重要）

```
┌─────────────────────────────┐
│ b2b-marketing-agent-harness │  Why / What / Decide
│ Agent · Workflow · Content  │  PostgreSQL
└──────────────┬──────────────┘
               │ Tool / API / Event
┌──────────────▼──────────────┐
│ crawl-worker-redis          │  How to collect
│ Queue · Worker · Fetch      │  MySQL
│ Parse · Retry · DLQ         │
└─────────────────────────────┘
```

- Harness 侧定义 `Crawler` 接口（CreateJob / GetJob / GetResults），通过 Adapter 调 crawler API。
- 未来可替换为 Firecrawl / Browserbase / Apify / 自建 crawler，Harness 不改。
- **禁止**：Harness 直接读写 crawler 的 MySQL；两个数据库不互相 JOIN。

---

## 13. 里程碑

| 里程碑 | 内容 | 状态 |
|--------|------|------|
| M0 | 需求 + 架构 + schema.sql + 项目骨架 | 进行中 |
| M1 | Harness 核心 + Mock 全链路 + REST API | 计划 |
| M2 | 6 个 Agent 参考实现 + 每日情报 Workflow | 计划 |
| M3 | 审批 / Policy / 可观测时间线 + 后台控制台 | 计划 |
| M4 | Crawler Tool 接真实 crawl-worker-redis | 计划 |
| M5 | LinkedIn Company Page API 真实发布 | 计划 |
| M6 | 评估 Harness（Eval / Guardrail / 回归）+ 调度器 | 计划 |

---

## 14. 术语表

| 术语 | 含义 |
|------|------|
| Harness | Agent 运行脚手架（Runtime + Registry + Memory + Knowledge + Policy + Workflow） |
| Agent | 具备目标、可调用 Tool、可反思的智能体 |
| Tool | Agent 可调用的原子能力（crawler/linkedin/content/...） |
| Workflow | 决定“何时/按何顺序”调用 Agent 的编排 |
| Policy | 决定某动作是否允许、是否需审批 |
| Approval | Human-in-the-loop 审批单 |
| Memory | Agent/用户/工作区“记得什么” |
| Knowledge | “世界上有什么知识”（文档/分块/实体/关系） |
| Run / Step | 一次 Agent 执行 / 执行中的一步 |
| Channel | 发布渠道（LinkedIn/官网/YouTube/...） |
